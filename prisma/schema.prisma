datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-py"
}

model HandRaisingSchema {
  studentId  String   @id
  lectureId  String   @id
  timestamp  DateTime @id
  handRaised Boolean
}